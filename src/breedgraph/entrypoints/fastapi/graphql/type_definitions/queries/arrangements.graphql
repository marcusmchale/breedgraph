type Layout {
    id: ID!
    type: LayoutType!
    name: String
    axes: [String!]
    arrangement: Layout!
    parent: Layout
    """Position within parent layout"""
    position: [String!]
    children: [Layout!]
    location: Location
}
