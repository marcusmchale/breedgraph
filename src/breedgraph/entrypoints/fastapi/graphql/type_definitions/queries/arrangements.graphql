type Layout {
    id: Int!
    type: OntologyEntryInterface!
    name: String
    axes: [String!]
    parent: Layout
    """Position within parent layout"""
    position: [String!]
    children: [Layout!]
}
