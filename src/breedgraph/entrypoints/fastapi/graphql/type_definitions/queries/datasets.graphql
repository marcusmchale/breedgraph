type Record {
    id: Int!
    unit: Unit
    value: String
    start: DateTime
    end: DateTime
    references: [Int]
    submitted: DateTime
}

type DataSet {
    id: Int!
    term: OntologyEntry
    records: [Record]
    contributors: [Int]
    references: [Int]
}
