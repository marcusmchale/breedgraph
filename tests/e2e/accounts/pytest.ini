# NOTE! these tests should only be run on a fresh empty db

[pytest]
addopts = --strict-markers
markers =
    asyncio
env =
    DATABASE_NAME=neo4j
    #DATABASE_NAME=empty  # No multi-db support for community edition,
